import { Metadata } from 'next'
import React from 'react'

export const metadata: Metadata = {
    title: 'Work in Progress',
}

const ReturnsPolicy = () => {
    return (
        <section className='pageWrapper min-h-screen '>Returns policy</section>
    )
}

export default ReturnsPolicy
